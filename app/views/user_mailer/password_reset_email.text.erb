Reset your password
===================

Hi<%= " #{@user.name}" if @user.name.present? %>,

We received a request to reset the password for your Inventory account.
Choose a new password here:

<%= @url %>

This link expires in <%= @ttl_hours %> hours. If you didn't request a
password reset, you can ignore this email — your password won't change.
